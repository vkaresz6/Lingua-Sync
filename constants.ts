import { PaneLayout, Segment } from './types';

export const INITIAL_PANE_LAYOUT: PaneLayout = {
    sidebar: ['chat'],
    bottom: ['machine-translation', 'evaluation', 'term-db', 'translation-memory', 'preview', 'sxDev']
};

export const INITIAL_SEGMENTS: Segment[] = [];